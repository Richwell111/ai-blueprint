# Findings

> **Generated file.** The findings ledger: review findings raised by `/audit`
> against the work in progress, each with a durable ID, severity (P0-P3), and
> status. `/implement` marks repaired findings `fixed`, a later `/audit` pass
> moves them to `closed`, and `/complete` refuses to merge while any P0 or P1
> finding is `open` or `fixed`, then archives resolved findings with the work
> and resets this file.

### F-01 [P2] fixed - Misplaced eslint-disable doesn't suppress the rule it targets

**File:** components/certificate/BlackBorderCertificate.tsx:72
**Found:** 2026-08-13 by /audit (scope: current)
**Why it matters:** The comment explains a deliberate choice (plain `<img>`,
not `next/image`, since Puppeteer renders this component server-side), but
`// eslint-disable-next-line` on line 72 disables line 73 (`return (`), not
the `<img>` element on line 74. `npm run lint` confirms this: it reports both
an "unused eslint-disable directive" (line 72) and the `no-img-element`
warning it was meant to suppress (line 74) - the intended suppression never
took effect.
**Suggested fix:** Move the `// eslint-disable-next-line @next/next/no-img-element`
comment to immediately precede the `<img` line (currently line 74).
**Resolution:** Moved the comment to immediately precede the `<img` element
while touching this function for the certificate design fix. `npm run lint`
now reports zero warnings.

### F-02 [P3] open - Geist Mono is loaded and preloaded but never used

**File:** app/layout.tsx:10
**Found:** 2026-08-13 by /audit (scope: current)
**Why it matters:** `Geist_Mono` is loaded via `next/font/google` and its
variable is applied to `<html>`, but no `font-mono` utility or `--font-mono`
usage appears anywhere in the current UI (`app/globals.css` maps
`--font-mono` but nothing consumes it yet). This was also visible as a
browser console warning during `/check`: the font file preloads but goes
unused within a few seconds of load, wasting a network request on every page
load.
**Suggested fix:** Remove the `Geist_Mono` import/loading from `app/layout.tsx`
until something actually needs a monospace font, or apply `font-mono`
somewhere if one is already planned.
**Resolution:**

### F-03 [P3] open - Page metadata is still create-next-app boilerplate

**File:** app/layout.tsx:15
**Found:** 2026-08-13 by /audit (scope: current)
**Why it matters:** The browser tab still shows "Create Next App" /
"Generated by create next app" even though `app/page.tsx` renders the real
CertiCreate certificate. Feature 2 (form + live preview, app-chrome layout)
has since shipped without touching `app/layout.tsx` either, so this is no
longer a speculative "maybe deferred" - it's genuinely still open.
**Suggested fix:** Update `metadata.title` / `metadata.description` to
reflect CertiCreate. Small enough to fold into the next feature that touches
`app/layout.tsx`, or do as a standalone `/fix`.
**Resolution:**
